import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  title: {
    color: '#fff',
    fontSize: 24,
  },
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
